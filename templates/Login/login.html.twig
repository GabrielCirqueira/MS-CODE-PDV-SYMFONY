{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}

<div class="login-wrapper">
  <div class="container-login row">
    <div class="left-panel col-md-6 d-flex flex-column align-items-center justify-content-center text-center">
      <h2 class="welcome-title">Bem-vindo</h2>
      <p class="welcome-text">Para continuar conectado conosco, por favor, faça login com suas informações pessoais.</p>
    </div>

    <div class="right-panel col-md-6 d-flex flex-column align-items-center justify-content-center">
      <h2 class="login-title">Login</h2>
      <p class="login-subtitle">Ou use seu e-mail para cadastro:</p>
      <form method="post" class="login-form w-100 px-4">
        <div class="mb-3">
          <input type="email" class="form-control login-input" id="username" name="email" placeholder="name@example.com" autocomplete="email" required autofocus>
        </div>
        <div class="mb-3">
          <input type="password" class="form-control login-input" id="password" name="senha" placeholder="Senha" autocomplete="current-password" required>
        </div>

        {% include 'flash.html.twig' %}
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('logar') }}">
        
        <button type="submit" class="btn btn-primary w-100 py-2 login-btn">Acessar</button>
      </form>
    </div>
  </div>
</div>


{% endblock %}
